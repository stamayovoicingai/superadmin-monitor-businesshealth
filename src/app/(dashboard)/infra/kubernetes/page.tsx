import { ComingSoon } from "@/components/coming-soon";

export default function KubernetesPage() {
  return (
    <ComingSoon
      title="Kubernetes"
      description="Cluster / pods / containers usage, requests & limits, restarts and logs."
      prd="PRD/06 §1"
    />
  );
}
