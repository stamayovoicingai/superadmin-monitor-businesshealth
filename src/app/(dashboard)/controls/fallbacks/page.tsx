import { ComingSoon } from "@/components/coming-soon";

export default function FallbacksPage() {
  return (
    <ComingSoon
      title="Fallback Controls"
      description="STT / TTS single fallback and LLM cost-ordered fallback list."
      prd="PRD/08"
    />
  );
}
