import { ComingSoon } from "@/components/coming-soon";

export default function FlagQueuePage() {
  return (
    <ComingSoon
      title="Flag Queue"
      description="Unified review queue for manually flagged and auto-flagged calls."
      prd="PRD/10"
    />
  );
}
